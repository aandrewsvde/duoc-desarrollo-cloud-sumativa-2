server:
  port: 8080

aws:
  s3:
    bucket-name: guias-despacho-aandrews
    region: us-east-1
  credentials:
    access-key: ${AWS_ACCESS_KEY_ID}
    secret-key: ${AWS_SECRET_ACCESS_KEY}
    session-token: ${AWS_SESSION_TOKEN}

spring:
  application:
    name: api-guias
